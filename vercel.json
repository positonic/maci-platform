{
  "version": 2,
  "installCommand": "npx pnpm@9 install",
  "builds": [
    {
      "src": "packages/interface/package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "echo 'skip install'",
        "buildCommand": "npx pnpm@9 run build"
      }
    }
  ]
}
